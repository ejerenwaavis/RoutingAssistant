<!doctype html>
<html lang="en">

<head>
  <meta charset="utf-8">
  <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
  <meta name="description" content="">
  <meta name="author" content="ACED Devops">
  <meta name="generator" content="Jekyll v4.1.1">
  <title>
    <%= body.title %> · RouteAssistant - TCS
  </title>

  <!-- Favicon -->
  <link rel="icon" href="https://triumphcourier.com/img/Logo-SVG.svg">


  <style>
    .bd-placeholder-img {
      font-size: 1.125rem;
      text-anchor: middle;
      -webkit-user-select: none;
      -moz-user-select: none;
      user-select: none;
    }

    @media (min-width: 768px) {
      .bd-placeholder-img-lg {
        font-size: 3.5rem;
      }
    }
  </style>

  <!-- Bootstrap core CSS -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.0-beta2/dist/css/bootstrap.min.css" rel="stylesheet"
    integrity="sha384-BmbxuPwQa2lc/FVzBcNJ7UAyJxM6wuqIj61tLrc4wSX0szH/Ev+nYRRuWlolflfl" crossorigin="anonymous">

  <!-- FontAwesome -->
  <script src="https://kit.fontawesome.com/5f30a8a83b.js" crossorigin="anonymous"></script>

  <!-- Stripe JS -->
  <script src="https://js.stripe.com/v3/"></script>

  <!-- Global Custom styles for Triumph Courier -->
  <link href="<%= body.publicFiles %>/css/style.css" rel="stylesheet">

  <!-- Custom styles for this template -->
  <link href="<%= body.publicFiles %>/css/login.css" rel="stylesheet">

</head>

<body class="text-center">



  <% if(body.title !=="Account" ){%>
    <%if(user){%>
      <div class="my-4 fixed-top">
        <a href="" class="btn btn-xs btn-outline-secondary dropdown-toggle" role="button" id="profileMenu"
          data-bs-toggle="dropdown" aria-expanded="false">
          <% if(user.photoURL){ %>
            <img src="<%=user.photoURL %>" alt="" class="profile-icon">
            <% } else { %>
              <div class="profile-icon bg-secondary">
                <i class="fas fa-1x fa-user mt-2 text-white"></i>
              </div>
              <% } %>

                <%= user.firstName %>
        </a>
        <ul class="dropdown-menu" aria-labelledby="dropdownMenuLink">
          <li><a class="dropdown-item" href="https://triumphcourier.com/">TCS Home</a></li>
          <li><a class="dropdown-item" href="<%= body.domain %>/">File Upload</a></li>
          <!-- <li><a class="dropdown-item" href="<%= body.domain %>/profile">Acount</a></li> -->
          <li><a class="dropdown-item" href="<%= body.domain %>/logout">Log Out <i class="fas fa-sign-out-alt"></i></a>
          </li>
          <% if(user.isProUser) {%>
            <hr>
            <li><a class="dropdown-item" href="<%= body.domain %>/brandsFileUpload">Brand Upload</a></li>
            <% } %>
        </ul>
      </div>
      <%}%>
        <%} else {%>
          <a href="<%= body.domain %>/" class="btn btn-xs btn-outline-secondary "><i class="fas fa-home"></i>
            Home</a>
          <%}%>





            <!-- *******************DOMAIN HANDLING*************************** -->
            <span class="d-none" id="domain" domain="<%= body.domain %>">
            </span>
 
           